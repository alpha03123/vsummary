class AgentPlanError(ValueError):
    """Agent 行动规划阶段的校验错误。

    当 LLM 生成的动作计划（工具调用列表、参数、引用等）无法通过业务
    规则校验时抛出——例如引用了不存在的证据条目、目标工具不可用、
    必填参数缺失等。继承自 `ValueError` 以便上层既可针对性捕获
    也可走通用的「参数错误」分支。
    """

    pass
